# Milkonomy 未提交改动总汇（2026-08-29）

> 基线：`main` @ `febe90f`（已推送，与 origin 同步）。
> 本文档聚合其后**全部未提交改动**，每个文件给出代码 + 参考文档（每段代码对应什么功能）。
> 提交前建议跑：`npm run test`（vitest）+ `npm run build:public`（vue-tsc 类型检查）。

## 改动全景

| # | 文件 | 类型 | 所属功能块 |
|---|------|------|-----------|
| 1 | `src/pinia/stores/game.ts` | 修改 | A 市场数据回退 + B 超炼缓存 |
| 2 | `src/pages/dashboard/components/GameInfo.vue` | 修改 | A |
| 3 | `.github/workflows/update-market.yml` | **新增** | A |
| 4 | `.github/workflows/update-data.yml` | 修改 | A |
| 5 | `scripts/fetch_game_data.py` | 修改 | A |
| 6 | `public/data/market.json` | 修改(数据) | A |
| 7 | `src/calculator/superAlchemy.ts` | **新增** | B 超级炼金 |
| 8 | `src/pages/super-alchemy/index.vue` | **新增** | B |
| 9 | `src/router/routes/private.ts` | 修改 | B |
| 10 | `src/locales/lang/en.ts` / `zh-tw.ts` | 修改 | A + B |
| 11 | `types/auto/components.d.ts` | 修改(自动生成) | B |
| 12 | `src/common/apis/manualchemy/index.ts` | 修改 | C 手动炼金增强 |
| 13 | `src/pages/manualchemy/index.vue` | 修改 | C |
| 14 | `src/pages/dashboard/components/ActionConfig.vue` | 修改 | D 小改动 |
| 15 | `mobile.scss` / `AppMain` / `LeftMode` | 修改 | D 移动端滚动 |

---

# 功能块 A：国内玩家无数据（"Invalid Date"）修复

**现象**：国内浏览器打开站点无数据、页脚显示英文 "Invalid Date"；QQ/手机QQ 内置浏览器正常。
**根因**：市场数据靠浏览器直连 `milkywayidle.com/game_data/marketplace.json`，国内不可达/DNS 劫持，重试 5 次全失败且无本地缓存 → `marketData = null` → `new Date(NaN)` 渲染成 "Invalid Date"。

## A1. `src/pinia/stores/game.ts`（核心）

### 参考文档（按行段）

| 行段（约） | 功能 |
|---|---|
| L4 | 引入超炼行类型，供 B 块的 `superAlchemyCache` 使用 |
| L112-114 | 新增状态：`manualchemyCache` 的 key 从时间戳数字改为字符串（带税率标志）；新增 `superAlchemyCache` |
| L143 | `initialize` 重试循环里 `fetchData()` 不再传 offset（旧逻辑用 offset 轮换 URL 数组，URL 数组已删） |
| L159-175 | `fetchData()` 重写：URL 选择 + 并行 fetch + 超时 + 回退（见下方代码注释） |
| L234-257 | manualchemy 缓存改带 key 存取；新增超炼缓存三件套 get/set/clear |
| L317 | `clearAllCaches()` 追加清超炼缓存 |
| L327-351 | 模块级工具函数：`fetchJson`（带超时的 fetch+JSON）、`fetchMarketSnapshot`（校验官方格式的快照回退） |

### 代码：fetchData 重写（L156 起）

```ts
async fetchData() {
  const url = import.meta.env.MODE === "development" ? "/" : "./"
  // 开发环境市场数据走 vite 代理（见 vite.config.ts /milkyway-market），浏览器直连官方站国内常失败
  const marketUrl = import.meta.env.MODE === "development" ? "/milkyway-market" : "https://www.milkywayidle.com/game_data/marketplace.json"
  // 官方站不可达（国内被墙/DNS劫持返回劫持页）时，回退到同源快照 data/market.json，
  // 该快照由 .github/workflows/update-market.yml 每小时从官方站抓取写入 gh-pages，
  // 玩家能打开本站就一定能取到同源快照
  const marketFallbackUrl = `${url}data/market.json`
  const DATA_URL = `${url}data/data.json`

  // marketplace.json 响应带 Cache-Control: public + 约 1 小时的 Expires，
  // 默认 fetch 会长期命中 HTTP 缓存，导致挂机时 5 分钟轮询反复拿到同一份数据，
  // 时间戳停在首次打开的时刻，必须绕过缓存
  const [newGameData, newMarketData] = await Promise.all([
    fetchJson<GameData>(DATA_URL, 15_000),
    fetchJson<MarketDataPlain>(marketUrl, 10_000, { cache: "no-store" })
      .catch(() => fetchMarketSnapshot(marketFallbackUrl))
  ])
  // ……后续"仅当 gameData 版本变化才更新状态并清缓存"逻辑未变
}
```

### 代码：缓存 key 化 + 超炼缓存（L231 起）

```ts
getManualchemyCache(key?: string) {
  const cacheKey = key ?? String(this.marketData!.timestamp)
  return this.manualchemyCache[cacheKey]
},
setManualchemyCache(list: Calculator[], key?: string) {
  this.clearManualchemyCache()
  this.manualchemyCache[key ?? String(this.marketData!.timestamp)] = list
},
clearManualchemyCache() {
  this.manualchemyCache = {}
},
getSuperAlchemyCache(key?: string) {
  const cacheKey = key ?? String(this.marketData!.timestamp)
  return this.superAlchemyCache[cacheKey]
},
setSuperAlchemyCache(list: SuperAlchemyRow[], key?: string) {
  this.clearSuperAlchemyCache()
  this.superAlchemyCache[key ?? String(this.marketData!.timestamp)] = list
},
clearSuperAlchemyCache() {
  this.superAlchemyCache = {}
},
```

> 为什么 key 化：同一份市场数据下"含税/不含税"是两套计算结果，旧实现只按 timestamp 存，切税率开关会拿到另一套的脏缓存。B/C 两块共用这个模式。

### 代码：新工具函数（文件末尾）

```ts
/** 带超时的 fetch + JSON 解析。官方站被 DNS 劫持返回 200 + HTML 时，json() 解析失败同样会抛错，由调用方回退 */
async function fetchJson<T>(url: string, timeoutMs: number, init?: RequestInit): Promise<T> {
  const controller = new AbortController()
  const timer = setTimeout(() => controller.abort(), timeoutMs)
  try {
    const response = await fetch(url, { ...init, signal: controller.signal })
    if (!response.ok) {
      throw new Error(`Response not ok: ${response.status}`)
    }
    return await response.json() as T
  } finally {
    clearTimeout(timer)
  }
}

/** 校验同源快照必须是官方 marketplace.json 格式（timestamp + marketData），
 * 防止历史上的 MWIApi 旧格式（market/time 键）混入后生成一份全空的市场数据 */
async function fetchMarketSnapshot(url: string): Promise<MarketDataPlain> {
  const data = await fetchJson<MarketDataPlain>(url, 15_000)
  if (!data || typeof data.timestamp !== "number" || !data.marketData) {
    throw new Error("Invalid market snapshot format")
  }
  return data
}
```

## A2. `src/pages/dashboard/components/GameInfo.vue`（页脚显示）

### 参考文档

| 行段 | 功能 |
|---|---|
| L7-10 | `marketTimeText`：timestamp 存在才 `new Date()`，否则显示"暂无数据"——杜绝 NaN 渲染成英文 "Invalid Date" |
| L11-14 | `isMarketFresh`：数据 2 小时内算新鲜（success 绿色），否则 error 红色；从模板搬进 computed，模板不再直接算 |
| L23-26 | 模板改用上面两个 computed |

### 代码（改动后的 script + 模板核心）

```vue
const marketTimeText = computed(() => {
  const ts = store.marketData?.timestamp
  return ts ? new Date(ts * 1000).toLocaleString() : t("暂无数据")
})
const isMarketFresh = computed(() => {
  const ts = store.marketData?.timestamp
  return !!ts && ts * 1000 > Date.now() - 1000 * 60 * 120
})
```

```html
<div :class="isMarketFresh ? 'success' : 'error'">
  <a href="...">{{ t('市场数据来源(MilkyWayIdle)') }} : {{ marketTimeText }}</a>
</div>
```

## A3. `.github/workflows/update-market.yml`（新增，全文 45 行）

### 参考文档

| 行段 | 功能 |
|---|---|
| L4-7 | 触发：每小时 :23 cron + 支持手动 workflow_dispatch |
| L9-10 | 需要 contents: write 权限才能推 gh-pages |
| L16-21 | 只 checkout gh-pages 分支（快照存在 gh-pages，不在 main） |
| L23-27 | 配置 git 身份和带 token 的 remote |
| L29-34 | curl 官方 marketplace.json → `data/market.json`，并用 python 校验必须是官方格式（timestamp 数字 + marketData 非空），格式不对直接 fail，防止写坏回退源 |
| L36-45 | 有变化才 commit + push，commit 带 `[skip ci]` 防自触发 |

### 代码（全文）

```yaml
name: Update Market Data
# 每小时抓取官方 marketplace.json 写入 gh-pages 的 data/market.json，
# 作为前端官方直连失败（国内被墙/DNS劫持）时的同源回退数据源（见 src/pinia/stores/game.ts fetchData）
on:
  schedule:
    - cron: "23 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-market:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout gh-pages branch
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: gh-pages
          fetch-depth: 1

      - name: Configure Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git

      - name: Fetch official marketplace.json
        shell: bash
        run: |
          mkdir -p data
          curl -fsSL --retry 5 --retry-delay 10 https://www.milkywayidle.com/game_data/marketplace.json -o data/market.json
          python3 -c "import json; d=json.load(open('data/market.json')); assert isinstance(d.get('timestamp'), (int, float)) and d.get('marketData'), 'invalid marketplace.json format'"

      - name: Commit and push if changed
        shell: bash
        run: |
          if git diff --quiet -- data/market.json; then
            echo "No market data changes"
            exit 0
          fi
          git add data/market.json
          git commit -m "Update market snapshot [skip ci]"
          git push origin gh-pages
```

## A4. `.github/workflows/update-data.yml`（不再覆盖快照）

### 参考文档

gh-pages 上的 `data/market.json` 现在由 update-market.yml 独家维护（官方格式）。本 workflow 原来会把 main 分支 `public/data/market.json` 复制过去——main 上这份是给"本地开发/构建回退"用的快照，格式可能滞后，覆盖过去反而把新鲜快照冲掉。因此三处删掉 market.json：checkout 行、cp 行、git add 行，只保留 data.json。

### 代码（改动后核心段）

```yaml
      - name: Copy data files from main
        shell: bash
        run: |
          git checkout origin/main -- public/data/data.json
          mkdir -p data
          cp public/data/data.json data/data.json
          rm -rf public

      - name: Commit and push if changed
        shell: bash
        run: |
          if git diff --quiet -- data/data.json; then
            echo "No data changes detected"
            exit 0
          fi
          git add data/data.json
          git commit -m "Update data files from main"
          git push origin gh-pages
```

## A5. `scripts/fetch_game_data.py`（本地抓数据脚本）

### 参考文档

| 行段 | 功能 |
|---|---|
| DATA_URL 列表 | 市场数据源从 MWIApi（旧格式 market/time）换成官方 marketplace.json——与前端 `fetchMarketSnapshot` 的格式校验配套，否则抓出来的是会被拒的旧格式 |
| 打印两行 | `time` 键换成 `time or timestamp` 兼容官方格式的字段名 |

### 代码

```python
DATA_URL = [
    "https://raw.githubusercontent.com/silent1b/MWIData/main/init_client_info.json",
    # 市场快照必须用官方 marketplace.json 格式（timestamp + marketData），
    # 前端 fetchMarketSnapshot 会校验该格式；MWIApi 旧格式（market/time）会被拒绝
    "https://www.milkywayidle.com/game_data/marketplace.json",
]
```

```python
            print(f"New Time: {new_data.get('time') or new_data.get('timestamp')}")
            print(f"Old Time: {existing_data.get('time') or existing_data.get('timestamp')}")
```

## A6. `public/data/market.json`（数据，不贴内容）

已刷新为**官方格式**快照（872 物品）。它是 main 分支里兜底的静态快照；线上真正新鲜的快照由 update-market.yml 每小时写入 gh-pages。18k 行 JSON 无需人工审查。

**A 块上线步骤**：push 后到 GitHub Actions 手动跑一次 "Update Market Data"，让 gh-pages 快照立即就位（不等整点 cron）。

---

# 功能块 B：超级炼金排行榜（新页面）

**一句话**：选定起点物品后，自动评估"分解/转化/点金"多步链条的最优处置（每步比直卖划算才继续炼），按整链利润/h 排行，点"查看"展开整棵决策树。
**核心设计**：所有量按"每 1 件物品"归一 → 决策与数量无关 → 全局记忆化复用（一次计算服务排行榜所有行 + 详情树）。

## B1. `src/calculator/superAlchemy.ts`（新增，全文 393 行）

### 参考文档（按行段）

| 行段 | 功能 |
|---|---|
| L9-12 | `SuperAction` 四种处置；`SuperAlchemyMode`：smart=每节点取净收益最高处置；longest=只要继续炼不亏就往深走 |
| L18-46 | `SuperUnitEval`：单物品链条评估结果（每 1 件归一）。关键字段：`costPerUnit` 本步附加成本（不含本体，中间产物沿链内部转移不重复计价）、`timePerUnit` 全链耗时、`reqLevel` 全链最高炼金等级要求（=物品 itemLevel）、`unitNet` 沿链净收益、`children` 期望子产物、`cycleCutProducts` 因转化环被切断按直卖计的产物、`mainPath` 主线动作序列（火车式展示） |
| L48-60 | `SuperAlchemyRow`：排行榜一行 = 物品 + 评估 + 购入价(ask) + 利润/利润h/利润率 + 起点成交量 |
| L62-66 | `SuperAlchemyOptions`：催化剂档位数组、卖出税率、模式 |
| L69-102 | `SuperNode`/`SuperTreeResult`：详情树节点（数量按链路缩放）与汇总 |
| L104-106 | 保险丝：memo 超 6000 项不再展开新分支；期望数量 < 0.005 的产物忽略 |
| L108-112 | 三种炼金动作的构造器表（分解/转化/点金） |
| L120-124 | `sellValueOf`：卖价（bid × 税率），金币按面值 1 |
| L126-139 | `calcOf`：构造单个计算器并跑一遍；**本体原料（第 0 位）之外的原料（金币/催化剂/茶）任一无市价 → 该动作不可用**；本体购入价在排行榜入口单独校验 |
| L146-274 | **核心 `unitEval`**：递归 + 记忆化评估单个物品。流程见下 |
| L276-279 | `hasAlchemyDetail`：物品是否有任何炼金分支 |
| L282-305 | `computeSuperAlchemy`：排行榜入口。遍历所有可交易且有炼金分支的物品，要求有卖单（ask>0）、最优处置不是纯直卖，算 profit = unitNet − ask，按利润/h 降序 |
| L308-393 | `buildSuperTree`：由 memo 结果重建某物品的展示树（数量按链路缩放），walk 一遍汇总总收益/成本/耗时/步数/深度 |

### `unitEval` 流程说明（L146-274）

1. memo 命中直接返回（这就是"一次计算服务全站"的来源）。
2. 构造"直卖"候选 `sellEv` 作为保底。
3. `inProgress.add(hrid)` —— **转化环切断**：产物命中"正在展开的路径"（如 golem↔twilight↔abyssal 精华互转）时按直卖计、不再递归，记入 `cycleCutProducts`。
4. 对每种动作 × 每档催化剂：算每 1 件的本步附加成本与耗时；遍历产物——金币按面值、环切断产物按直卖、其余递归 `unitEval` 累加 net 和耗时。
5. `mainPath` = 本步动作 + "贡献净收益最大的可炼子节点"的主线（火车式"3步分解+2步转化"的来源）。
6. 选优：longest 模式在"不比直卖亏"的炼金候选里取最高 net（链能长则长）；smart 模式全体候选取最高 net。
7. `finally` 里 `inProgress.delete(hrid)` 保证异常时也不污染环检测。

### 代码（全文见仓库文件，此处为便于离线阅读的关键段落）

```ts
const ACTION_CTORS: { action: SuperAction, create: (hrid: string, catalystRank: number) => Calculator }[] = [
  { action: "decompose", create: (hrid, catalystRank) => new DecomposeCalculator({ hrid, catalystRank }) },
  { action: "transmute", create: (hrid, catalystRank) => new TransmuteCalculator({ hrid, catalystRank }) },
  { action: "coinify", create: (hrid, catalystRank) => new CoinifyCalculator({ hrid, catalystRank }) }
]
```

```ts
function unitEval(hrid: string, ctx: Ctx): SuperUnitEval {
  const cached = ctx.memo.get(hrid)
  if (cached) return cached
  // ……直卖保底 sellEv ……
  ctx.inProgress.add(hrid)
  try {
    for (const { action } of ACTION_CTORS) {
      for (const catalystRank of options.catalystRanks) {
        const calc = calcOf(action, hrid, catalystRank, taxFactor)
        if (!calc) continue
        const ingredients = calc.ingredientListWithPrice
        const main = ingredients[0]
        const actionsPerItem = 1 / main.count          // 每 1 件物品对应的动作次数
        const costPerUnit = /* 非本体原料 count×price 求和 */ 0 * actionsPerItem
        const ownTimePerUnit = (3600 / calc.actionsPH) * actionsPerItem
        for (const product of calc.productListWithPrice) {
          // 金币按面值 / 环切断按直卖 / 其余进 children 递归
        }
        // unitNet = cutIncome - costPerUnit + Σ childEv.unitNet × countPerUnit
        // timePerUnit = ownTimePerUnit + Σ childEv.timePerUnit × countPerUnit
        // mainPath = [action, ...贡献净收益最大可炼子节点的主线]
        // costAllPerUnit / reqLevel 沿子链累计
      }
    }
  } finally {
    ctx.inProgress.delete(hrid)
  }
  // 选优：longest 只在"不比直卖亏"的候选里挑；smart 全体挑；写入 memo
}
```

```ts
export function computeSuperAlchemy(options: SuperAlchemyOptions): SuperAlchemyRow[] {
  const ctx: Ctx = { options, memo: new Map(), inProgress: new Set() }
  const rows: SuperAlchemyRow[] = []
  for (const item of Object.values(getGameDataApi().itemDetailMap)) {
    if (!item.isTradable || !hasAlchemyDetail(item)) continue
    const ask = getUsedPriceOf(item.hrid, 0, "ask") ?? -1
    if (ask < 0) continue            // 市场无卖单，链条无从买起
    const ev = unitEval(item.hrid, ctx)
    if (ev.action === "sell") continue // 最优处置就是直卖，无炼金意义
    const profit = ev.unitNet - ask
    const cost = ask + ev.costAllPerUnit
    rows.push({
      item, eval: ev, ask, profit,
      profitPH: ev.timePerUnit > 0 ? (profit / ev.timePerUnit) * 3600 : profit,
      profitRate: cost > 0 ? profit / cost : 0,
      volume1h: getPriceOf(item.hrid, 0).vol ?? -1
    })
  }
  rows.sort((a, b) => b.profitPH - a.profitPH)
  return rows
}
```

> 完整 393 行源码在 `src/calculator/superAlchemy.ts`，每个字段/常量都有中文行内注释，可直接对照阅读。

## B2. `src/pages/super-alchemy/index.vue`（新增页面，全文 421 行）

### 参考文档（按行段）

| 行段 | 功能 |
|---|---|
| L24-33 | 筛选状态（全部 useMemory 持久化）：搜索词/动作筛选/催化剂档/含税开关/模式/最小步数/最低等级/最低利润率/最低成交量/排除装备 |
| L41-70 | `load()`：组装 options → 拼**缓存 key**（市场 timestamp + 税标志 + 催化剂档 + 模式 + 买卖价状态 + 玩家配置版本）→ 未命中才 `computeSuperAlchemy` 并写缓存 → 50ms setTimeout 让 loading 转起来 |
| L72-80 | 触发重算的 watch：催化剂/税率/模式/市场数据/玩家配置/买卖价状态任一变化 |
| L88-109 | 前端筛选（不重算）：动作/排除装备/步数/等级/利润率/成交量/搜索词 + 三列自定义排序（利润h、利润、利润率） |
| L111-121 | 前端分页 + 筛选后页码越界回 1 |
| L126-131 | 四种动作的标签配色（卖出 info / 分解 warning / 转化 success / 点金 danger） |
| L133-142 | `pathText`：连续同动作合并计数，渲染成"3步分解+2步转化"火车式文案 |
| L144-146 | `playerAlchemyLevel`：读玩家炼金等级，用于"要求等级超了标红" |
| L148-153 | `formatSec`：秒 → s/m/h 自适应格式化 |
| L158-172 | 详情弹窗：`buildSuperTree` 建树 → `flatNodes` 拍平成带 depth 的数组（表格 + 缩进模拟树） |
| L178-189 | 顶栏：GameInfo + ActionConfig(只留炼金) + 价格状态选择 + 含税开关 |
| L193-236 | 卡片头：全部筛选项 + 催化剂（自动/无/1/2）+ 模式（智能/最长链）单选组 |
| L239-300 | 排行表格：图标/物品/路径/步数/要求等级(超标红)/利润h/利润件/利润率(负值红)/耗时件/购入价/成交量/查看 |
| L318-400 | 详情弹窗：descriptions 汇总（总收益/总成本/购入价/利润/总耗时/利润h/步骤数/深度）+ 成本口径说明 + 缩进树表格（数量/动作/耗时/附加成本/直卖价值/链条净收益/备注：转化环切断、无市价） |

> 完整 421 行源码在 `src/pages/super-alchemy/index.vue`。

## B3. `src/router/routes/private.ts`（路由）

### 参考文档

新增 `/super-alchemy` 路由（Layouts 下子路由，菜单标题"超级炼金"，图标复用 dashboard）。**注意**：该路由块 `path: "/"` 且 `redirect: "/super-alchemy"`——登录后根路径直接进超级炼金。若不想让超炼当首页，去掉这个 redirect 或把路由块改为独立前缀（如 `/super`）。

### 代码

```ts
{
  path: "/",
  component: Layouts,
  redirect: "/super-alchemy",
  children: [
    {
      path: "super-alchemy",
      component: () => import("@/pages/super-alchemy/index.vue"),
      name: "SuperAlchemy",
      meta: {
        title: t("超级炼金"),
        svgIcon: "dashboard"
      }
    }
  ]
},
```

## B4. 多语言 `src/locales/lang/en.ts` / `zh-tw.ts`

### 参考文档

en.ts 新增约 30 个 key、zh-tw.ts 新增约 40 个（zh-tw 多几个通用词如 搜索/查看/详情/总成本）。内容 = 超炼页面全部 UI 文案 + A 块的"暂无数据"。key 即中文原文，value 为译文，机制无改动。代表性条目：

```ts
// en.ts
"暂无数据": "No data yet",
"超级炼金": "Super Alchemy",
"智能": "Smart",
"最长链": "Longest Chain",
"转化环自动按直卖切断": "Transmute loops are cut off and sold directly",
"成本口径：中间产物沿链内部转移不重复计价；……": "Cost model: intermediate products transfer along the chain without double counting; ..."
```

## B5. `types/auto/components.d.ts`

unplugin-vue-components 自动生成：超炼页面用到了 ElBadge/ElDescriptions/ElDescriptionsItem，自动登记。**不要手改**，构建时自动更新。

---

# 功能块 C：手动炼金页增强

## C1. `src/common/apis/manualchemy/index.ts`

### 参考文档（按行段）

| 行段 | 功能 |
|---|---|
| 新 import | `NO_TAX_FACTOR`/`SELL_TAX_FACTOR`（税率常量）、`handleVolume1hSearch`（成交量区间过滤，已存在于 utils.ts L112） |
| `getLeaderboardDataApi` 开头 | 解析 `includeTax`（默认 true）→ 税率因子；**缓存 key = timestamp + 税标志**（切开关拿另一套缓存，不重算） |
| 计算分支 | `calcAllFlowProfit(sellTaxFactor)` 把税率传下去 |
| 计算后新增过滤 | ① `actionLevel` 要求等级下限；② `maxItemLevel` 物品等级上限（要求是 number 且 ≤ 上限）；③ `handleVolume1hSearch` 成交量(1h) 区间 |
| `calcAllFlowProfit` | 签名加 `sellTaxFactor` 形参，透传给 `calcManualchemyProfit` |
| `calcManualchemyProfit` | `new WorkflowCalculator(configs, name, sellTaxFactor)`——构造器第三参卖税率（workflow.ts L51 已有此参数，此前走默认值） |

### 代码（核心改动）

```ts
export async function getLeaderboardDataApi(params: Leaderboard.RequestData) {
  const includeTax = params.includeTax !== false
  const sellTaxFactor = includeTax ? SELL_TAX_FACTOR : NO_TAX_FACTOR
  // 税率不同结果不同，缓存 key 必须带税标志
  const cacheKey = `${useGameStoreOutside().marketData!.timestamp}-${includeTax ? "tax" : "noTax"}`
  let profitList: Calculator[] = []
  const cached = useGameStoreOutside().getManualchemyCache(cacheKey)
  if (cached && cached.length > 0) {
    profitList = cached
  } else {
    /* ……计算并 setManualchemyCache(profitList, cacheKey)…… */
  }
  profitList = profitList.filter(item => item.actionLevel >= (params.actionLevel || 0))
  const hasMaxItemLevel = params.maxItemLevel !== undefined && params.maxItemLevel !== null
  if (hasMaxItemLevel) {
    const maxItemLevel = Number(params.maxItemLevel)
    profitList = profitList.filter((item) => {
      const itemLevel = item.item?.itemLevel
      return typeof itemLevel === "number" && itemLevel <= maxItemLevel
    })
  }
  profitList = handleVolume1hSearch(profitList, params)
  return handlePage(handleSort(handleSearch(profitList, params), params), params)
}
```

## C2. `src/pages/manualchemy/index.vue`

### 参考文档（按行段）

| 行段 | 功能 |
|---|---|
| L60-68 | 搜索状态新增：actionLevel / maxItemLevel / minVolume1h / maxVolume1h / banCharm（useMemory 自动持久化） |
| L71 | `includeTax` 独立持久化开关（默认 true） |
| L74-84 | 请求参数带上 includeTax；watch 触发源追加 includeTax（切税率立即重查） |
| L239-241 | 顶栏新增「计算税率」复选框，change 即触发搜索 |
| L276-317 | 筛选区新增 5 个控件：要求等级≥ / 物品等级≤ / 成交量(1h)≥ / 成交量(1h)≤ / 排除护符复选框 |
| 其余 ~150 行 | **纯格式化重排**（引号统一双引号、缩进修正、模板标签拆行）+ 一处事件名修正：`@toggleCompare` → `@toggle-compare`（Vue 3 emit 需 kebab-case 监听 camelCase 事件），逻辑零变化，无需逐行审 |

---

# 功能块 D：小改动

## D1. `src/pages/dashboard/components/ActionConfig.vue`

### 参考文档

| 行段 | 功能 |
|---|---|
| L52-67 | **固定特殊装备"无"三态改造**：`fixedIconHridOf`（hrid 为空=无，图标用该类型默认物品置灰显示）；`isFixedSpecialOff`（无 hrid 或强化等级≤0 都算"关"）；`onFixedSpecialToggle`（勾"无"→等级清 0；取消勾 → 补默认 hrid + 等级设 10） |
| L507 | **安装脚本链接醒目化**：弹窗里「安装脚本」链接加 `color:#409eff;text-decoration:underline;font-weight:600`（蓝色+下划线+加粗），用户反馈之前看不出能点 |
| L876-881 | **特殊装备导入"是啥就是啥"**：一键导入时不再用默认配置兜底填充特殊装备，8 个槽位先全部置为 hrid=""（无），导入了什么就是什么，没有就保持"无" |
| L1327-1339 | 模板配合改造：图标/勾选框改用上面三个函数 |

### 代码（导入逻辑改造）

```ts
// 填充特殊装备（头、脚、手、戒指、项链、耳环、副手、背包）— 导入是啥就是啥，没有就是"无"，不补默认装备
const specialEquipMap = new Map<Equipment, PlayerEquipmentItem>()
for (const se of DEFAULT_SEPCIAL_EQUIPMENT_LIST) {
  specialEquipMap.set(se.type, { type: se.type, hrid: "", enhanceLevel: undefined })
}
```

### 代码（链接醒目化）

```ts
ElMessageBox.alert(
  "<p>检测到未安装数据导出脚本。</p><p style=\"margin-top:8px\">📥 " +
  "<a href=\"https://home.greasyfork.org.cn/zh-hans/info#/zh-CN/scripts/587094/detail\" target=\"_blank\" " +
  "style=\"color:#409eff;text-decoration:underline;font-weight:600\">安装脚本</a></p>" +
  "<p style=\"margin-top:8px;color:#909399\">安装后刷新 MilkyWay Idle 页面，再回利润网点「一键导入」。</p>",
  t("未安装脚本"), { dangerouslyUseHTMLString: true, confirmButtonText: t("知道了") }
)
```

## D2. 移动端整页滚动修复（3 文件配套）

### 参考文档

问题：手机浏览器 `100vh` 含地址栏高度 > 可视区 → 文档被撑出滚动余量 + 滚动链传播 → 手指拖动时整个页面（含顶栏）跟着滚。修法三件套：

| 文件 | 功能 |
|---|---|
| `src/common/assets/styles/mobile.scss` L11-18 | ≤992px 时锁死 html/body/#app `overflow:hidden`——文档不可滚，只剩内容区滚（断点与 useResize 的 MAX_MOBILE_WIDTH 一致） |
| `src/layouts/components/AppMain/index.vue` L40-41 | `.app-scrollbar` 加 `overscroll-behavior: contain`——内容区滚到边界不把滚动链传给文档 |
| `src/layouts/modes/LeftMode.vue` L125-126 | `height: 100vh` 后追加 `height: 100dvh`——动态视口高度，随地址栏收放 |

### 代码

```scss
// mobile.scss
@media (max-width: 992px) {
  html,
  body,
  #app {
    overflow: hidden;
  }
}
```

```scss
// AppMain/index.vue .app-scrollbar
overscroll-behavior: contain;
```

```scss
// LeftMode.vue .fixed-header + .app-main
height: 100vh;
height: 100dvh; // 手机上 100vh 含地址栏高度、大于可视区，会把文档撑出滚动余量
```

---

# 其他未跟踪文件

- `HANDOFF-v2.5.0-20260816.md`、`HANDOFF-税率5%.md`：历史交接文档，随本次一起提交或移出仓库均可。

# 提交建议

1. `npm run test` + `npm run build:public` 全绿后再提交。
2. 可按功能块分 3 个 commit（A 修复 / B+C 超炼与手炼增强 / D 小改动），或合 1 个。
3. push 后手动触发 "Update Market Data" workflow（A 块上线关键步骤）。
4. **待真机验证**：D2 移动端滚动修复需手机实际体验确认。
